using UnityEngine;
using System.Collections;

public class Crew : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution (800, 1600, true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
